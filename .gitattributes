# Normalize line endings to LF in the repository.
# Docker and Python tooling on Linux expect LF; this prevents CRLF from
# sneaking in when contributors work on Windows.
* text=auto eol=lf

# Binaries — no line ending conversion.
*.png binary
*.jpg binary
*.ico binary
*.pdf binary
